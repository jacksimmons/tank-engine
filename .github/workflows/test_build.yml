name: Build Compatibility Tests
on:
  push:
    branches: [ main, audio ]
jobs:
  build-on-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - name: GMake (GCC) build
        uses: actions/checkout@v5
        run: ./gmake_build.sh
