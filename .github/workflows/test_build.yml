name: Build Compatibility Tests
on:
  push:
    branches: [ main, audio ]
jobs:
  build-on-ubuntu:
    runs-on: ubuntu-latest
    uses: actions/checkout@v5
    steps:
      - run: ls
      - name: GMake (GCC) build
        run: ./gmake_build.sh
